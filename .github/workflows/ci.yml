on:
  - pull_request
  - push

name: "Windows"

jobs:

  windows:
    name: "Tests (Windows)"

    defaults:
      run:
        shell: bash

    runs-on: ubuntu-latest

    steps:
      - name: "Checkout"
        uses: actions/checkout@v3

      - name: "phpinfo"
        run: php -i
